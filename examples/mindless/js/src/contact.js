define(function(require){

  // This file will be output to `seajs_build.options.outputPath`,
  // if `seajs_build.options.buildType` is not `merge_only`
  console.log("This file is not used by anyone.");

  require( "src/utils.js" );
});
